#include <bits/stdc++.h>
using namespace std;
#define ll long long
#define ull unsigned long long
vector <int> team;

int main() {
	ios::sync_with_stdio(0);
	cout.tie(0); cin.tie(0);
	
	cin >> n >> k;
	team.resize(n+7);
	
	for (int i = 0; i<n; i++)
		cin >> team[i];
		
	sort(team.begin(), team.end());
	
	for (int i = 1; i<=n; i++){
		while
	}
	
	
}


