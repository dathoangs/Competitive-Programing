#include <bits/stdc++.h>
#define ll long long
#define ull unsigned long long
#define pb push_back
#define null NULL
using namespace std;
int n, m, k, x;
string s;
string s1;
vector <int> post;

int main() {
	ios::sync_with_stdio(0);
	cout.tie(0); cin.tie(0);
	cin >> n >> m >> k >> x;
	
	cin >> s;
	for (int i = 0; i<s.length(); i++){
		if (post.size() = m)
			break;
			
		if (s[i] == '#')
			post.pb(i);
	}
	
	for (int i = 0; i<x; i++){
		int tmp = 0;
		while (tmp)
	}
	
}




























